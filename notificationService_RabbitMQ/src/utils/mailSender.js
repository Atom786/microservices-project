const sendNotification = (email, message) => {
    console.log(`📩 Sending email to ${email}: ${message}`);
    // Replace this with real email service later (SendGrid, SMTP, etc.)
  };
  
  module.exports = sendNotification;
  